const sbThemes=window.supabase.createClient(BB_CONFIG.supabaseUrl,BB_CONFIG.supabasePublishableKey);function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}async function loadThemes(){const target=document.getElementById('themeGrid'),q=(document.getElementById('themeSearch')?.value||'').trim().toLowerCase();const {data,error}=await sbThemes.from('tags').select('id,name,book_tags(book_id)').order('name');if(error){target.innerHTML='<div class="empty">Não foi possível carregar os temas.</div>';return}const rows=(data||[]).filter(t=>!q||t.name.toLowerCase().includes(q));target.innerHTML=rows.length?rows.map(t=>`<a class="category-card" href="tema.html?id=${encodeURIComponent(t.id)}"><div class="eyebrow">TEMA</div><h2>${esc(t.name)}</h2><p>${(t.book_tags||[]).length} livro(s) relacionado(s)</p></a>`).join(''):'<div class="empty">Nenhum tema encontrado.</div>'}document.addEventListener('DOMContentLoaded',loadThemes);