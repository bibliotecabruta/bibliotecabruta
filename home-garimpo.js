const sbHome=window.supabase.createClient(BB_CONFIG.supabaseUrl,BB_CONFIG.supabasePublishableKey);function hEsc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}async function renderHomeGarimpo(){const [catsRes,tagsRes]=await Promise.all([sbHome.from('categories').select('id,name,area').order('name'),sbHome.from('tags').select('id,name').order('name')]);const cats=catsRes.data||[],tags=tagsRes.data||[];const catBox=document.getElementById('homeCategories'),tagBox=document.getElementById('homeTags');if(catBox)catBox.innerHTML=cats.length?cats.map(c=>`<a class="chip category-link" href="catalogo.html?area=${encodeURIComponent(c.area)}&category=${encodeURIComponent(c.id)}">${hEsc(c.name)}</a>`).join(''):'<span class="muted">Nenhuma categoria cadastrada.</span>';if(tagBox)tagBox.innerHTML=tags.length?tags.map(t=>`<a class="chip tag-link" href="catalogo.html?tag=${encodeURIComponent(t.id)}">${hEsc(t.name)}</a>`).join(''):'<span class="muted">Nenhum tema cadastrado.</span>'}document.addEventListener('DOMContentLoaded',renderHomeGarimpo);