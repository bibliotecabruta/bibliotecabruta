const sbCategory=window.supabase.createClient(BB_CONFIG.supabaseUrl,BB_CONFIG.supabasePublishableKey);function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}async function loadCategory(){const root=document.getElementById('categoryPage'),id=new URLSearchParams(location.search).get('id');if(!id){root.innerHTML='<div class="empty">Categoria não informada.</div>';return}const {data:c,error}=await sbCategory.from('categories').select('id,name,area,book_categories(books(id,title,cover_url,series_volume,authors(id,name),series(id,name),editions(cover_url,is_primary,country,publisher,publication_year)))').eq('id',id).single();if(error||!c){root.innerHTML='<div class="empty">Categoria não encontrada.</div>';return}document.title=`${c.name} — Biblioteca Bruta`;const books=(c.book_categories||[]).map(x=>x.books).filter(Boolean).sort((a,b)=>a.title.localeCompare(b.title));root.innerHTML=`<a class="backlink" href="categorias.html">← Categorias e temas</a><section class="series-hero"><div class="eyebrow">${esc(c.area)}</div><h1>${esc(c.name)}</h1><div class="series-summary"><strong>${books.length} ${books.length===1?'livro relacionado':'livros relacionados'}</strong></div></section><section class="detail-section"><h2>Livros</h2><div class="series-volume-grid">${books.map(b=>{const ed=(b.editions||[]).find(e=>e.country==='Brasil'&&e.is_primary)||(b.editions||[]).find(e=>e.country==='Brasil');const cover=ed?.cover_url||b.cover_url;const pub=[ed?.publisher,ed?.publication_year].filter(Boolean).join(' • ');return `<a class="volume-card" href="livro.html?id=${encodeURIComponent(b.id)}">${cover?`<img src="${esc(cover)}" alt="${esc(b.title)}">`:'<div class="mini-placeholder">Sem capa</div>'}<div><strong>${esc(b.title)}</strong><span><a class="entity-text-link" href="autor.html?id=${encodeURIComponent(b.authors?.id||'')}">${esc(b.authors?.name||'Autor não informado')}</a></span>${pub?`<span>${esc(pub)}</span>`:''}${b.series?.name?`<span>${esc(b.series.name)}</span>`:''}</div></a>`}).join('')}</div></section>`}document.addEventListener('DOMContentLoaded',loadCategory);