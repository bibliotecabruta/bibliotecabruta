const sbAuthors=window.supabase.createClient(BB_CONFIG.supabaseUrl,BB_CONFIG.supabasePublishableKey);
function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
async function renderAuthors(){const root=document.getElementById('authorsList'),q=(document.getElementById('authorSearch')?.value||'').trim().toLowerCase();const {data,error}=await sbAuthors.from('authors').select('id,name,nationality,book_authors(books(id,title,area))').order('name');if(error){root.innerHTML='<div class="empty">Não foi possível carregar os autores.</div>';return}const rows=(data||[]).map(a=>({...a,linkedBooks:(a.book_authors||[]).map(x=>x.books).filter(Boolean)})).filter(a=>a.linkedBooks.length&&(!q||a.name.toLowerCase().includes(q)));root.innerHTML=rows.length?rows.map(a=>{const areas=[...new Set(a.linkedBooks.map(b=>b.area).filter(Boolean))];return `<a class="entity-card" href="autor.html?id=${encodeURIComponent(a.id)}"><div><div class="eyebrow">AUTOR</div><h2>${esc(a.name)}</h2>${a.nationality?`<p>${esc(a.nationality)}</p>`:''}</div><div class="entity-meta"><strong>${a.linkedBooks.length} ${a.linkedBooks.length===1?'livro':'livros'}</strong><span>${areas.map(esc).join(' • ')}</span></div></a>`}).join(''):'<div class="empty">Nenhum autor encontrado.</div>'}
document.addEventListener('DOMContentLoaded',renderAuthors);
