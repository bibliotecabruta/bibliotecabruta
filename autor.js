const sbAuthor=window.supabase.createClient(BB_CONFIG.supabaseUrl,BB_CONFIG.supabasePublishableKey);function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}async function loadAuthor(){const root=document.getElementById('authorPage'),id=new URLSearchParams(location.search).get('id');if(!id){root.innerHTML='<div class="empty">Autor não informado.</div>';return}const {data:a,error}=await sbAuthor.from('authors').select('id,name,nationality,bio,books(id,title,area,cover_url,series_volume,series(id,name),editions(cover_url,is_primary,country,publisher,publication_year))').eq('id',id).single();if(error||!a){root.innerHTML='<div class="empty">Autor não encontrado.</div>';return}document.title=`${a.name} — Biblioteca Bruta`;const books=(a.books||[]).sort((x,y)=>String(x.series?.name||'').localeCompare(String(y.series?.name||''))||(x.series_volume||999)-(y.series_volume||999)||x.title.localeCompare(y.title));const seriesMap=new Map();books.forEach(b=>{if(b.series?.id&&!seriesMap.has(b.series.id))seriesMap.set(b.series.id,b.series)});root.innerHTML=`<a class="backlink" href="autores.html">← Todos os autores</a><section class="series-hero"><div class="eyebrow">AUTOR</div><h1>${esc(a.name)}</h1>${a.nationality?`<p class="muted">${esc(a.nationality)}</p>`:''}${a.bio?`<p>${esc(a.bio)}</p>`:''}<div class="series-summary"><strong>${books.length} ${books.length===1?'livro no catálogo':'livros no catálogo'}</strong>${seriesMap.size?`<span class="badge">${seriesMap.size} ${seriesMap.size===1?'série':'séries'}</span>`:''}</div></section>${seriesMap.size?`<section class="detail-section"><h2>Séries</h2><div class="chips">${[...seriesMap.values()].map(s=>`<a class="chip entity-text-link" href="serie.html?id=${encodeURIComponent(s.id)}">${esc(s.name)}</a>`).join('')}</div></section>`:''}<section class="detail-section"><h2>Livros publicados no Brasil</h2><div class="series-volume-grid">${books.map(b=>{const ed=(b.editions||[]).find(e=>e.country==='Brasil'&&e.is_primary)||(b.editions||[]).find(e=>e.country==='Brasil');const c=ed?.cover_url||b.cover_url;const pub=[ed?.publisher,ed?.publication_year].filter(Boolean).join(' • ');return `<a class="volume-card" href="livro.html?id=${encodeURIComponent(b.id)}">${c?`<img src="${esc(c)}" alt="${esc(b.title)}">`:'<div class="mini-placeholder">Sem capa</div>'}<div>${b.series_volume?`<small>VOL. ${esc(b.series_volume)}</small>`:''}<strong>${esc(b.title)}</strong>${pub?`<span>${esc(pub)}</span>`:''}${b.series?.name?`<span>${esc(b.series.name)}</span>`:''}</div></a>`}).join('')}</div></section>`}document.addEventListener('DOMContentLoaded',loadAuthor);