const sbCategories=window.supabase.createClient(BB_CONFIG.supabaseUrl,BB_CONFIG.supabasePublishableKey);function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}async function renderCategories(){const root=document.getElementById('categoriesList'),q=(document.getElementById('categorySearch')?.value||'').trim().toLowerCase(),area=document.getElementById('areaFilter')?.value||'';const {data,error}=await sbCategories.from('categories').select('id,name,area,book_categories(book_id)').order('name');if(error){root.innerHTML='<div class="empty">Não foi possível carregar as categorias.</div>';return}const rows=(data||[]).filter(c=>(c.book_categories||[]).length&&(!q||c.name.toLowerCase().includes(q))&&(!area||c.area===area));root.innerHTML=rows.length?rows.map(c=>`<a class="entity-card" href="categoria.html?id=${encodeURIComponent(c.id)}"><div><div class="eyebrow">${esc(c.area)}</div><h2>${esc(c.name)}</h2></div><div class="entity-meta"><strong>${c.book_categories.length} ${c.book_categories.length===1?'livro':'livros'}</strong><span>Ver catálogo relacionado →</span></div></a>`).join(''):'<div class="empty">Nenhuma categoria encontrada.</div>'}document.addEventListener('DOMContentLoaded',renderCategories);